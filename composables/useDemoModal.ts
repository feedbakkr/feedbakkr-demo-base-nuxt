/**
 * Shared local-storage key used by every feedbakkr-demo-base-* repo to
 * remember that a visitor has dismissed the first-run demo modal.
 */
export const DEMO_MODAL_ACK_KEY = 'feedbakkr-demo-ack'
